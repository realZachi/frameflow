# Required only for AI-assisted screenshot generation.
# Create a key at https://platform.kimi.ai/console/api-keys and copy this file
# to .env.local. Never use a VITE_ prefix: Vite exposes VITE_* variables to
# browser code.
MOONSHOT_API_KEY=
